import React, { Component } from 'react';
import LoginPage from './LoginPage';

class Home extends Component {

    render() {
        return (
                    <LoginPage />
        );
    }
}

export default Home;